# Running YCSB workloads

All relevant configuration is stored in `/ycsb/conf/`.
To run a workload, run:

```
docker compose run --rm ycsb <dbms> <workload>
```

todo
explain env files in each directory
